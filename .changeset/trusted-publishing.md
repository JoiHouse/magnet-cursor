---
'@joihouse/magnet-cursor-core': patch
'@joihouse/magnet-cursor-react': patch
'@joihouse/magnet-cursor-vue': patch
---

Publish through npm trusted publishing (OIDC) instead of a long-lived token. No runtime changes; the
tarballs carry the same code as 0.1.0 with provenance attached by the trusted publisher.
