<!--
  Behaviour changes belong in `packages/core`; the framework packages stay thin.
  See CONTRIBUTING.md if this is your first change here.
-->

**What this changes**

**Why**

**How it was verified**
<!-- The commands you ran, and anything you checked by hand in a browser. -->

---

- [ ] `pnpm test` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes (`pnpm lint:fix` fixes what it can)
- [ ] `pnpm build` passes
- [ ] `pnpm check:exports` passes if `package.json` or the build config changed
- [ ] A changeset is included if this changes published behaviour (`pnpm changeset`)
- [ ] Documented: README and, for a new or renamed option, the API tables
